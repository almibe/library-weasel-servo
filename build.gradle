buildscript {
    repositories {
        mavenLocal()
        jcenter()
    }
    dependencies {
        classpath 'org.almibe:easy-pub-plugin:0.3.0'
        classpath 'org.libraryweasel:library-weasel-gradle-plugin:0.2.0-SNAPSHOT'
    }
}

apply plugin: 'org.almibe.easypubplugin'
apply plugin: 'org.libraryweasel.gradleplugin'

group 'org.libraryweasel'
version '0.1.0'

repositories {
    mavenLocal()
    jcenter()
}

dependencies {
    compile felixDependencyManagerDep
}

bundle {
    instruction 'Import-Package', '*'
    instruction 'Export-Package', 'org.libraryweasel.servo.*'
}
